// mapgen — MapGenerator interface, LevelBuilder, BSP/cellular/drunkard/prefab
// generators, and decorate (stairs, spawns, reachability) (§17). Lands in
// milestones 3 and 10 (§20). May import core; never rotJS/DOM.
export {};
