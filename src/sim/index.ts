// sim — the rules (§17). Timeline, action/effect pipeline, handlers, stats,
// resources, combat, status, factions, triggers, items, and the field AI.
// Lands across milestones 2, 4–6 (§20). May import core; never rotJS/DOM.
export {};
