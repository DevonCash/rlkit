// core — pure data + mixins (§17). Entity, component/mixin registries, World,
// Level, query layer, coords, events/reactors, RNG, tags, geometry, dice,
// weighted tables. Lands in milestone 1 (§20). No rotJS, no DOM imports.
export {};
